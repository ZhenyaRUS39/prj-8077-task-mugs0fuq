# Plan: prj-8077-task-mugs0fuq

**Project:** `prj-8077-task-mugs0fuq`  
**Task ID:** `task-mugs0fuq`  
**Repo:** `prj-8077-task-mugs0fuq`  

## Summary

План: создать структуру CLI-парсера nginx-логов (sub-1), реализовать парсинг access.log и формирование отчёта с метриками (sub-2), добавить юнит-тесты и валидацию на примере реального лога (sub-3).

## Subtasks

### 1. Создать структуру проекта и CLI-скелет

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Инициализировать Python-проект: pyproject.toml/requirements.txt, модуль nginx_log_parser/cli.py с argparse (параметры --log, --report, --top), пустой парсер и точка входа. Без бизнес-логики — только каркас.
- **Test plan:** Запустить `python -m nginx_log_parser --help` — отображается список аргументов без ошибок. Файлы cli.py, __main__.py, pyproject.toml существуют.

### 2. Реализовать парсинг access.log и сбор статистики

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Реализовать чтение логов nginx (combined формат: ip, datetime, request, status, size, referer, ua). Парсить строки регуляркой, считать: топ-N IP по количеству запросов, распределение статус-кодов, топ URL, суммарный трафик. Вывести в отчёт (text или json по флагу).
- **Test plan:** Прогнать на тестовом файле sample.log (минимум 100 строк с разными IP/status/url). Сверить вывод: топ-IP совпадает с ручным подсчётом, сумма размеров совпадает, статус-коды распределены корректно.
- **Dependencies:** `sub-1`

### 3. Покрыть тестами и добавить README с примером

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать pytest-тесты: успешный парсинг валидной строки, обработка битых строк (skip + warning), корректность подсчёта топ-N, агрегация по статусам. Добавить README.md с установкой, примером запуска и форматом вывода.
- **Test plan:** `pytest -q` — все тесты зелёные. README содержит секции Install, Usage (с примером команды), Output format. Запуск примера из README на sample.log даёт ожидаемый отчёт.
- **Dependencies:** `sub-2`


---
*Generated by Hermes Orchestrator*
